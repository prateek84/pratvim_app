# Pratvim Prototype

Open `index.html` locally to run the prototype.

Validation helper pages are included as `validate_*.html`.
Screenshot evidence is available in `screenshots/`.
See `VALIDATION_REPORT.md` for the full checklist.
